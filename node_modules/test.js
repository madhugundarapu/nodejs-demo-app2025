const request = require('supertest');
const express = require('express');
const app = express();

app.get('/', (req, res) => res.status(200).send('Hello, World! CI/CD pipeline is working 🚀'));

describe('GET /', () => {
  it('responds with Hello World message', (done) => {
    request(app)
      .get('/')
      .expect(200, done);
  });
});
